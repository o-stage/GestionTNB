<Window x:Class="TaxesV1.EditFile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" Width="800" Height="450" WindowStartupLocation="CenterOwner">
    <DockPanel>
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Button Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" />
        </Grid>

        <StackPanel Orientation="Vertical" Grid.RowSpan="3" DockPanel.Dock="Top">
            <TextBox />
            <TextBox />
            <TextBox />
            <TextBox />

        </StackPanel>
    </DockPanel>
</Window>